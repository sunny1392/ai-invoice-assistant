generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Invoice {
  id         String   @id @default(cuid())
  fileName   String
  filePath   String
  uploadedAt DateTime @default(now())
}